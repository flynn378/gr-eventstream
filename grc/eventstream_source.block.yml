# auto-generated by grc.converter

id: eventstream_source
label: EventStream Source
category: EVENTSTREAM

parameters:
-   id: nthreads
    label: Handler Threads
    dtype: int
    default: '1'
-   id: type
    label: IO Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part
-   id: num_streams
    label: Num Streams
    dtype: int
    default: '1'
    hide: part
-   id: eb
    label: Early Behavior
    dtype: enum
    options: [discard, balk, asap, rndf]
    option_labels: [DISCARD, BALK, ASAP, RNDF]
    option_attributes:
        raw: ['0', '1', '2', '3']
-   id: mm
    label: Merge Mode
    dtype: enum
    options: [memcpy, addint8s, addint16s, addfloats]
    option_labels: [MEMCPY, ADD_INT8S, ADD_INT16S, ADD_FLOATS]
    option_attributes:
        raw: ['0', '1', '2', '3']

inputs:
-   domain: message
    id: schedule_event
    optional: true

outputs:
-   domain: stream
    dtype: ${ type }
    vlen: 1
    multiplicity: ${ num_streams }
-   domain: message
    id: nproduced
    optional: true
-   domain: message
    id: events_scheduled
    optional: true

templates:
    imports: import eventstream as es
    make: es.source(${num_streams}*[${type.size}], ${nthreads}, ${eb.raw}, ${mm.raw})

file_format: 1
