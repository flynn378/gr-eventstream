# auto-generated by grc.converter

id: eventstream_trigger_edge_f
label: Trigger Rising Edge Event
category: EVENTSTREAM

parameters:
-   id: thresh
    label: Trigger Threshold
    dtype: real
    default: '1.0'
-   id: length
    label: Event Length (samples)
    dtype: int
    default: '100'
-   id: lookback
    label: Event Lookback (samples)
    dtype: int
    default: '0'
-   id: guard
    label: Minimum Trigger Distance (samples)
    dtype: int
    default: '300'
-   id: type
    label: Passthrough Data Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part

inputs:
-   label: thresh_input
    domain: stream
    dtype: float
-   label: passthru_in
    domain: stream
    dtype: ${ type }
    optional: true

outputs:
-   label: passthru_out
    domain: stream
    dtype: ${ type }
    optional: true
-   domain: message
    id: which_stream
    optional: true
-   domain: message
    id: edge_event
    optional: true
asserts:
- ${ lookback >= 0 }

templates:
    imports: import eventstream as es
    make: es.trigger_edge_f(${thresh},${length},${lookback},${type.size},${guard})
    callbacks:
    - self.${id}.set_thresh(${thresh})

file_format: 1
