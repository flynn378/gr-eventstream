# auto-generated by grc.converter

id: eventstream_variable_evt
label: Event-Time Parameter
category: EVENTSTREAM
flags:
- show_id

parameters:
-   id: label
    label: Label
    dtype: string
    hide: ${ ('none' if label else 'part') }
-   id: value
    label: Default Value
    dtype: ${ type.type }
    default: '0'
-   id: type
    label: Type
    dtype: enum
    options: ['', complex, eng_float, intx, long, string]
    option_labels: [None, Complex, Float, Int, Long, String]
    option_attributes:
        type: [raw, complex, real, int, int, string]
    hide: ${ ('none' if type else 'part') }
-   id: req
    label: Event Param Required
    dtype: enum
    options: ['True', 'False']
value: ${ value }

templates:
    var_make: self.${id} = ${id} = ${value}

documentation: |-
    ES Event Parameters are parameters passed to an event when it occurs,
    If they are undefined in the event instance a default may be specified or an assertion behavior may be throw.

file_format: 1
