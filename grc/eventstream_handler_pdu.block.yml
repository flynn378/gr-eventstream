# auto-generated by grc.converter

id: eventstream_handler_pdu
label: Handler to PDU
category: EVENTSTREAM

parameters:
-   id: dt
    label: Data Type
    dtype: enum
    options: [es.es_handler_print.TYPE_F32, es.es_handler_print.TYPE_C32]
    option_labels: [Float 32, Complex Float (2x32)]

inputs:
-   domain: message
    id: handle_event
    optional: true

outputs:
-   domain: message
    id: pdus_out
    optional: true

templates:
    imports: import eventstream as es
    make: es.es_make_handler_pdu(${dt})

documentation: |-
    This handler converts stream events into PDUs

file_format: 1
