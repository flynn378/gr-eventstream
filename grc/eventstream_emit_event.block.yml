# auto-generated by grc.converter

id: eventstream_hier_event_emit
label: Hier Stream Event - Emitted Event
category: EVENTSTREAM

parameters:
-   id: evtname
    label: Event Symbol Key
    dtype: string
    default: EMITTED_EVENT_1

templates:
    imports: |-
        import eventstream as es;
        import pmt;
    make: self.${id} = ${id} = ${evtname}

documentation: |-
    This block may be placed inside a Hierarchical Stream Event
    It indicates that the stream event may emit an event of the given type
    Typically the event queue to post the new event to is passed as a parameter

file_format: 1
