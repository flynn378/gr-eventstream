# auto-generated by grc.converter

id: eventstream_arb
label: EventStream Arbiter
category: EVENTSTREAM
flags:
- show_id

parameters:
-   id: value
    label: value
    dtype: string
    default: ARB OK
-   id: hash
    label: initial hash
    dtype: raw
    default: '{''someval'':1, ''someval2'':2}'
value: ${ value }

templates:
    imports: import eventstream as es
    var_make: self.${id} = ${id} = es.es_make_arbiter()

file_format: 1
