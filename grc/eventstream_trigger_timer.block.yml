# auto-generated by grc.converter

id: eventstream_timer_trigger
label: Trigger System-Timer Event
category: EVENTSTREAM

parameters:
-   id: length
    label: Event Length
    dtype: int
    default: '5000'
-   id: delay
    label: Delay Seconds
    dtype: real
    default: '1.0'
-   id: iterations
    label: Num Iterations
    dtype: int
    default: '-1'

outputs:
-   domain: message
    id: which_stream
    optional: true
-   domain: message
    id: timer_event
    optional: true

templates:
    imports: import eventstream as es
    make: "\\\nes.es_trigger_timer(\"TIMER_EXPIRED_EVENT\", ${length}, ${delay}, ${iterations}); "

documentation: "This block generates events on timer expiration\n    \n    delay -\
    \ float, time in seconds\n\n    iterations - number of iterations before stopping,\
    \ -1 for infinite"

file_format: 1
