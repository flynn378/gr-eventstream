# auto-generated by grc.converter

id: parameter_eventstream_init
label: EVENTSTREAM Init Parameter

parameters:
-   id: label
    label: Label
    dtype: string
    hide: ${ ('none' if label else 'part') }
-   id: value
    label: Value
    dtype: ${ type.type }
    default: '0'
-   id: type
    label: Type
    dtype: enum
    options: ['', complex, eng_float, intx, long, string]
    option_labels: [None, Complex, Float, Int, Long, String]
    option_attributes:
        type: [raw, complex, real, int, int, string]
    hide: ${ ('none' if type else 'part') }
-   id: short_id
    label: Short ID
    dtype: string
    hide: '${ #if not type

        all#slurp

        #elif short_id

        none#slurp

        #else

        part#slurp

        #end if }'
asserts:
- ${ len(short_id) in (0, 1) }
- ${ short_id == '' or short_id.isalpha() }

templates:
    var_make: self.${id} = ${id}
    make: ${value}

documentation: |-
    ES Init Parameters may be used to parameterize components within the underlying event.
    They are set once at start up.

file_format: 1
