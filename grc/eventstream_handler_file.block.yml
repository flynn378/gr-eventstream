# auto-generated by grc.converter

id: eventstream_handler_file
label: Handler File
category: EVENTSTREAM

parameters:
-   id: dt
    label: Data Type
    dtype: enum
    options: [es.es_handler_file.TYPE_F32, es.es_handler_file.TYPE_C32]
    option_labels: [Float 32, Complex Float (2x32)]
-   id: path
    label: Base Path
    dtype: string
    default: /tmp
-   id: desc
    label: Descrption
    dtype: string
    default: burst

inputs:
-   domain: message
    id: handle_event
    optional: true

templates:
    imports: import eventstream as es
    make: es.es_make_handler_file(${dt}, ${path}, ${desc})

documentation: |-
    This handler saves event data to files

file_format: 1
