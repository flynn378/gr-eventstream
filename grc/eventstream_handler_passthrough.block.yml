# auto-generated by grc.converter

id: eventstream_handler_passthrough
label: Handler Passthrough
category: EVENTSTREAM

inputs:
-   domain: message
    id: handle_event
    optional: true

outputs:
-   domain: message
    id: events_out
    optional: true

templates:
    imports: import eventstream as es
    make: es.es_make_handler_passthrough()

documentation: |-
    This handler passes an event pmt through to the next block.

file_format: 1
