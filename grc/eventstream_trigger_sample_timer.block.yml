# auto-generated by grc.converter

id: eventstream_trigger_sample_timer
label: Trigger Sample-Timer Event
category: EVENTSTREAM

parameters:
-   id: length
    label: Event Length (samples)
    dtype: int
    default: '100'
-   id: period
    label: Event Periodicity (samples)
    dtype: int
    default: '10000'
-   id: shift
    label: Periodic Event Offset From 0 (samples)
    dtype: int
    default: '5000'
-   id: sched_dist
    label: Schedule-Ahead Distance
    dtype: int
    default: '25000'
-   id: type
    label: Passthrough Data Type
    dtype: enum
    options: [complex, float, int, short, byte]
    option_attributes:
        size: [gr.sizeof_gr_complex, gr.sizeof_float, gr.sizeof_int, gr.sizeof_short,
            gr.sizeof_char]
    hide: part

inputs:
-   domain: stream
    dtype: ${ type }
    optional: true

outputs:
-   domain: stream
    dtype: ${ type }
    optional: true
-   domain: message
    id: which_stream
    optional: true
-   domain: message
    id: sample_timer_event
    optional: true

templates:
    imports: import eventstream as es
    make: es.trigger_sample_timer(${type.size}, ${period}, ${shift}, ${sched_dist},
        ${length} )

file_format: 1
